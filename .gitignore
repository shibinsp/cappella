# Original Figma export — kept as read-only backup, not served
Cappella Website.html

# macOS metadata
.DS_Store
**/.DS_Store

# Screenshots (dev only)
screenshots/

# Node / Playwright (test harness)
node_modules/
test-results/
playwright-report/
test-artifacts/
.vercel

# Session scratch (screenshots, pasted media) — never repo content
uploads/
